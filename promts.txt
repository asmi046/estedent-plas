________

В файле index.blade.php внутри @section('main') @endsection сверстай блок шапки сайта согласно следующего описания:

В качестве обертки используй тег header который будет во всю высоту экрана внутрь помести   <div class="container"> </div> внутри которого уже будет остальная разметка.

Тегу header присвой background-color из переменной  $gray_bg_color

Див с классом container  должен быть flex, space-betwin

Внутри дива с класоом  container 4 блока:

    - блок с логотипом. Формат: ссылка оборачивающая img картинку взять из public\img\logo.svg высота логотипа 60px ширина автоматическая
    - блок с адресом в него добавляем текст: Адрес: г.Курск <br>Павлуновского, 114 <br>Ежедневно с 09:00-21:00
    - блок с телефоном, телефон обернут в ссылку размер шрифта: 22px шрифт жирный
    - блок с кнопкой "Записаться" кнопка будет ссылкой ее стилизовать не нужно просто добавь класс button


Стили реализуй в scss, размести их в файле _header.scss, в этом файле стилей переменные импортируй из _vars.scss. Не используй переменные которых нет в файле _vars.scss.  Сделай адаптацию под устройства.

______

В файле index.blade.php после закрывающего тега header сверстай секцию согласно следующего описания согласно следующего описания:

В качестве обертки используй тег section который будет во всю высоту экрана внутрь помести   <div class="container"> </div> внутри которого уже будет остальная разметка.

Внутри дива с класоом  container должно быть расположено меню со следующими пунктами:

Услуги
Специалисты
Цены
Акции
Клиника
Контакты

Меню должно быть реализовано с применением тега nav. Пункты меню не должны сожержать определения классов, стилизуй их от родителя. Меню должно быть растянуто на всю ширену контейнера.

Стили реализуй в scss, размести их в файле _main_menu.scss, в этом файле стилей переменные импортируй из _vars.scss. Не используй переменные которых нет в файле _vars.scss.  Сделай адаптацию под устройства.
______

В файле index.blade.php после блока с классом main_menu_section сверстай секцию согласно следующего описания:

В качестве обертки используй тег section который будет во всю высоту экрана внутрь помести   <div class="container"> </div> внутри которого уже будет остальная разметка. Диву с классом container присвой фоновое изображение из файла public\img\main_b.svg.
Так же див с классом container должен быть flex, flex-wrap.

Внутри дива с класоом  container должно быть расположено 2 блока:
    - Левый блок (ширина 30%)
        -в нем расположен заголовок h1 с теством - Стоматологическая клиника Эстедент плюс
        - под блоком должны быть расположен параграф со следующим текстом - Мы дарить вам здоровую и красивую улыбку, которая станет вашим пропуском в мир успеха и уверенности
        - Под ним должен быть расположен список ul со следующими элементами "Передовые технологии и мастерство, отточенное временем", "Многолетняя репутация семейной стоматологии", "Опытные врачи со стажем 6-19 лет"
        - ниже списка расположенны 2 кнопки который должны быть реализованы через тег a. Текст на кнопках: 'Наши услуги', 'Наши врачи' соответственно. Кнопки стилизовать не нужно просто добавь класс button.

    - Правый блок (flex:1), в нем должен быть расположен swipper слайдер со следующими настройками:
        - при создании swiper используй подход WebComponent описанный в документации к нему
        - настроить swipper необходимо с помощью js пример инициализации ниже в задании, написанную инициализацию необходимо поместить в файл resources\js\sliders\main-banner-swiper.js
        - в видимой зоне отображаются 1 слайд внутри слайде изображение
        - добавь 2 слайда и  помести в них изображения public\tmp_data\slide_1.webp и public\tmp_data\slide_2.webp
        - слайдер не имеет пагинации, элементы управления будут кастомные

    Блок со слайдером должен иметь бордер с лева и сверху с параметрами '20px solid $sub_color' скругление левого верхнего угла 100px.

    Пример инициализации слайдера:

    // Импортируем Swiper
    import { register } from 'swiper/element/bundle';

    // Регистрируем Swiper элементы
    register();

    // Инициализация Swiper слайдера для секции МИП
    class MipSwiper {
        constructor() {
            this.init();
        }

        init() {
            // Ждем загрузки DOM
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', () => {
                    this.initSwiper();
                });
            } else {
                this.initSwiper();
            }
        }

        initSwiper() {
            const swiperContainer = document.querySelector('.mip-swiper');

            if (!swiperContainer) return;

            // Настройки Swiper
            const swiperParams = {
                slidesPerView: 2,
                spaceBetween: 30,
                loop: true,
                navigation: {
                    nextEl: '.slider-control--next',
                    prevEl: '.slider-control--prev',
                },
                breakpoints: {
                    0: {
                        slidesPerView: 1,
                        spaceBetween: 20,
                    },
                    640: {
                        slidesPerView: 2,
                        spaceBetween: 30,
                    }
                }
            };

            // Применяем настройки к swiper-container
            Object.assign(swiperContainer, swiperParams);

            // Инициализируем Swiper
            swiperContainer.initialize();
        }
    }

    // Экспортируем класс
    export default MipSwiper;



Стили реализуй в scss, размести их в файле _banner.scss, в этом файле стилей переменные импортируй из _vars.scss. Не используй переменные которых нет в файле _vars.scss.  Сделай адаптацию под устройства.


______

Сгенерируй стили в в scss для блока swiper_control_wrap расположенного в фале resources\views\index.blade.php и покажи их в чате. Стили формируй согласно описанию:

Блок swiper_control_wrap должен быть flex все элементы внутри должны быть с права. расстояние между элементами 20px.
Вложенный элемент swiper-control должен иметь цвет фона из переменной $sub_color при наведении менять цвет на $hover_color. у этого элемента должны быть закруглены правый нижний и левый верхний углы на 5px. Так же у этого элемента должно быть фоновое изображение которое нужно взять из файла public\img\icon\arrow.svg. размер фонового изображения 90%, расположение по центру.

элемент swiper-control с дополнительным  классом swiper-button-prev нужно отразить по горизонтали.
